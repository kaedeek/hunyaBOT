name: Discord Notify

on:
  push:
    branches:
      - main

jobs:
  notify:
    runs-on: ubuntu-latest

    steps:
      - name: Discord notifications
        uses: cl8dep/action-discord@0.1
        with:
          webhook: ${{ secrets.DISCORD_WEBHOOK }}
          message: |
            📦 **GitHub に更新がありました**
            リポジトリ: ${{ github.repository }}
            実行者: ${{ github.actor }}
            ブランチ: ${{ github.ref_name }}
            内容: ${{ github.event.head_commit.message }}
